#include <iostream>
using namespace std;

int main() 
{
    int a; 
    int b;
    int c;
    
    cin >> a >> b >>  c;
    cout << a * b * c << endl;
    cout << (2 * a * b) + (2 * b * c) + (2 * a * c) << endl;
}
