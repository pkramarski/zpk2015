#include <iostream>
using namespace std;

int main() 
{
    int a, b, k, licznik,pom;
    cin >> a >> b >> k; 
    licznik = 0;
    pom=k;
    while(pom<=b)
    {
        if(a%k==0)
        licznik++;
        pom=pom+k;
    }
    cout << licznik << endl;

    return 0;
}
