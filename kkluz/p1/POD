#include <iostream>
using namespace std;

int main() 
{
    int a, b, k, licznik,pom;
    cin >> a >> b >> k; 
    licznik = 0;
    pom=k;
    while(pom<=b)
    {
        if(pom>=a)
        licznik++;
        pom=pom+k;
    }
    cout << licznik << endl;

    return 0;
}
