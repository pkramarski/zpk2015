#include <iostream>
using namespace std;

int main() 
{
    int a, b, k, z, licznik = 0;
    cin >> a >> b >> k ;
    for(z = a; z >= a && z <= b; z++){
        if(z % k == 0){
            licznik++;}
    }
    cout << licznik << endl;
    return 0;
}
