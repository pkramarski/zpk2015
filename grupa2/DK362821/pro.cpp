#include <iostream>
using namespace std;

int main() {
    unsigned long int a, b, c;
      cin >> a >> b >> c;
        cout << a * b * c << endl;
        cout << 2 * a * b + 2 * a * c + 2 * b * c << endl;
}
